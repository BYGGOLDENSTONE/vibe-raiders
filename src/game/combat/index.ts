// Wave 2: damage application, hit detection, status effects, death handling.
// Subscribe to 'damage:dealt', mutate Health, emit 'entity:died' / 'mob:killed'.

import type { GameContext } from '../state';

export function initCombat(_ctx: GameContext): void {
  // implemented in Wave 2
}
