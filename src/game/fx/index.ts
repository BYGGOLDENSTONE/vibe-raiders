// Wave 2: post-processing (bloom, vignette, color grade), screenshake, hit-stop,
// particle pool, floating damage numbers. Subscribes to fx:* events.

import type { GameContext } from '../state';

export function initFx(_ctx: GameContext): void {
  // implemented in Wave 2
}
