// Wave 2: loot drops, procgen affix system, ground item spawn.
// Subscribes to 'mob:killed' → rolls drop table → spawns LootDrop entity in world.

import type { GameContext } from '../state';

export function initLoot(_ctx: GameContext): void {
  // implemented in Wave 2
}
