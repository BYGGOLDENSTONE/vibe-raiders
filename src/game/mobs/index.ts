// Wave 1: mob spawning + AI brain (idle/aggro/chase/attack/flee/dead).
// Visual: primitive composite (capsule torso + sphere head + box arms) per archetype.

import type { GameContext } from '../state';

export function initMobs(_ctx: GameContext): void {
  // implemented in Wave 1 by parallel agent
}
