// Wave 2: pickup detection, inventory grid UI, equipment swap, tooltip + comparison.

import type { GameContext } from '../state';

export function initInventory(_ctx: GameContext): void {
  // implemented in Wave 2
}
