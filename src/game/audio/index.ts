// Wave 3: procedural ambient drone + WebAudio SFX synthesis (no asset downloads).
// Subscribes to 'audio:sfx' events and damage/death events for reactive audio.

import type { GameContext } from '../state';

export function initAudio(_ctx: GameContext): void {
  // implemented in Wave 3
}
