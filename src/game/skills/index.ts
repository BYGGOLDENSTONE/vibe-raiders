// Wave 2: skill registry per class, cooldown management, cast pipeline.
// Subscribes to 'player:skillCast' events from input/, emits 'skill:cast' + spawns FX/projectiles.

import type { GameContext } from '../state';

export function initSkills(_ctx: GameContext): void {
  // implemented in Wave 2
}
