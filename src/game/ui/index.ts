// Wave 2: HUD — HP/Resource bars, hotbar, XP bar, level, minimap, FPS, party panel.
// All DOM under #ui-root. Subscribes to events for reactive updates.

import type { GameContext } from '../state';

export function initUI(_ctx: GameContext): void {
  // implemented in Wave 2
}
