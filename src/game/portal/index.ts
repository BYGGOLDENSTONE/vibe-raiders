// Wave 4: Vibe Jam 2026 webring portal entry/exit.
// Renders 2 in-world portals: an "outbound" one that links to vibej.am/portal/2026
// and a "return" arch when the player arrives via ?portal=true.

import type { GameContext } from '../state';

export function initPortal(_ctx: GameContext): void {
  // implemented in Wave 4
}
