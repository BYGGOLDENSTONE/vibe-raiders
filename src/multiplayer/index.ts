// Wave 4: PartyKit client wrapper.
// Connects to relay, sends position/rotation, renders ghost players from snapshots,
// handles party formation and dungeon room migration.

import type { GameContext } from '../game/state';

export function initMultiplayer(_ctx: GameContext): void {
  // implemented in Wave 4
}
